# Open Export DEF dialog
db::showExportDef
set dlg [gi::getDialogs dbExportDef]

# Make dialog active
gi::setActiveDialog $dlg

# Optional: set geometry
db::setAttr geometry -of $dlg -value 445x402+536+191

# Set view
gi::setField viewName -value layout -in $dlg

# Set output directory
gi::setField runDirectory \
  -value "/home/users/svgplayout2601yoatia/svgplayout2601yoatia_ws_14nm/testiiiiiiiiinnnggggg" \
  -in $dlg

# Set DEF file name
gi::setField fileName \
  -value "ffffffffffffffffffffffffffffffffff.def" \
  -in $dlg

# Run export
gi::pressButton ok -in $dlg
